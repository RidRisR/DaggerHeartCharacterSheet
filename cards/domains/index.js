// 自动生成的领域卡牌索引文件
const { 奥术_CARDS } = require('./奥术_cards');
const { 利刃_CARDS } = require('./利刃_cards');
const { 骸骨_CARDS } = require('./骸骨_cards');
const { 典籍_CARDS } = require('./典籍_cards');
const { 优雅_CARDS } = require('./优雅_cards');
const { 午夜_CARDS } = require('./午夜_cards');
const { 贤者_CARDS } = require('./贤者_cards');
const { 辉耀_CARDS } = require('./辉耀_cards');
const { 勇气_CARDS } = require('./勇气_cards');

module.exports = {
    奥术_CARDS,
    利刃_CARDS,
    骸骨_CARDS,
    典籍_CARDS,
    优雅_CARDS,
    午夜_CARDS,
    贤者_CARDS,
    辉耀_CARDS,
    勇气_CARDS
};